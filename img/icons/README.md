# POI アイコン

- **24 種の PNG** … 涙型ピンの円の中に白シルエットとして埋め込み
- **`dot.svg`** … 該当なし用（●）

定義は `data/poi-icons.json` を参照。

## 差し替え

同じファイル名で PNG を上書きすれば反映されます（`npm run dev` 中はブラウザを再読み込み）。

SVG は `dot.svg` のみ使用します（24 種は PNG）。
