# Gougle Map（架空）

実在の地図タイルや OSM / Google Maps データを使わない、静的 SVG ベースの架空日本風マップです。道路網、鉄道、河川、街区、建物、農地、山地を手元データとして配置し、実在しそうな見た目に寄せています。

`index.html?token=...` で表示します。

`token-gate.js` の `TOKEN_GATE_ORIGIN` を本番ポータルに合わせてください。

GitHub Pages ではこのリポジトリを `gougle-map` として公開してください。
